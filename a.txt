    ┌──────────────────────────────────────────────────────────────────────────────────────┐
    │                                   ESP32-P4 主控板                                    │
    │                                                                                      │
    │  ┌─ 传感器层 ────┐    ┌─ 执行层 ────┐  ┌─ 交互层 ──────┐  ┌─ 算法层 ──────────────┐  │
    │  │ SHT3x×2 室内外 │   │ 舵机×6      │  │ EK79007 1024×600│  │ 环境感知 安全→粗筛→投票 │  │
    │  │ BH1750  光照   │   │ 窗帘×4 GPIO  │  │ GT911 触控     │  │ 自适应预测 时间+传感器加权│  │
    │  │ ADS1115 ADC    │   │ 雨棚×2 GPIO  │  │ LVGL UI        │  │ 定时调度 每日+一次性定时 │  │
    │  │ 雨水+烟雾+气流  │   │ MCPWM 缓动   │  │ 中文 4000+ 字  │  │ 本地NLP 规则引擎 <1ms   │  │
    │  │ 风向 RMT       │   │              │  │                │  │ 自然风扫描 风向追踪      │  │
    │  └────────────────┘  └──────────────┘  └────────────────┘  └────────────────────────┘  │
    │                                                                                      │
    │  ┌─ 语音管线 (ESP-SR) ────────────────────────────────────────────────────────────┐  │
    │  │  MIC → ES8311 → WakeNet9("嗨ESP") → MultiNet7(5命令词) → ESP-TTS("小乐") → SPK │  │
    │  └────────────────────────────────────────────────────────────────────────────────┘  │
    │                                                                                      │
    │  ┌─ 消息总线 (msg_bus) ───────────────────────────────────────────────────────────┐  │
    │  │               WebSocket ←→ 传感器 ←→ 语音 ←→ 算法 ←→ UI ←→ 舵机                │  │
    │  └────────────────────────────────────────────────────────────────────────────────┘  │
    └────────────────────────────────┬─────────────────────────────────────────────────────┘
                            SDIO    │
                     ┌──────────────┴──────────────┐
                     │       ESP32-C6 WiFi         │
                     └──────────────┬──────────────┘
                                    │
                     ┌──────────────┴──────────────┐
                     │      relay_server.py        │
                     │     (PC 中转服务器)          │
                     ├─────────────────────────────┤
                     │ WebSocket 中继 + HTTP Web UI │
                     │                             │
                     │ → 阿里云 NUI   语音识别 ASR  │
                     │ → DeepSeek V4  语义理解 NLP  │
                     │ → Open-Meteo   天气预报      │
                     └──────────────┬──────────────┘
                                    │
            ┌───────────────────────┼───────────────────────┐
            ▼                       ▼                       ▼
      ┌──────────┐          ┌─────────────┐          ┌──────────┐
      │ 文字回复  │          │ ★ 大模型下发控制 │          │ 天气数据  │
      │ "好的~"  │          │              │          │ 晴 35°C  │
      └──────────┘          │ "三点开窗" → │          └──────────┘
                            │  {time,cmd, │
                            │   reply}    │
                            │     ↓       │
                            │ ESP32 创建  │
                            │ 一次性定时   │
                            │ 到时自动执行 │
                            └─────────────┘
